<form method="post" action="{{ path('app_back_author_delete', {'id': author.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn btn-outline-danger mt-3 mb-3">Delete</button>
</form>
