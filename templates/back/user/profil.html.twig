{% extends 'back.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <div class="col-12 p-3">
        <h1 class="h3">Mon profil</h1>
    </div>
    <div class="col-12">
        <div class="card card-body">
            <div class="col sm-5 m-3">
                <i class="bi bi-person"></i>
                <small>Bienvenue : </small><span class="fw-bold">{{ app.user.firstname }}</span>
            </div>
            <div class="table-responsive-sm">
                <table class="table table-hover">
                    <thead class="bg-light">
                        <tr>
                            <th><i class="bi bi-envelope"></i> Email</th>
                            <th>Roles</th>
                            <th>Prénom</th>
                            <th>Nom</th>
                            <th>actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>{{ app.user.email }}</td>
                            <td>{{ app.user.roles ? app.user.roles|json_encode : '' }}</td>
                            <td>{{ app.user.firstname }}</td>
                            <td>{{ app.user.lastname }}</td>
                            <td>
                                <a href="{{ path('app_back_user_show', {'id': app.user.id}) }}" class="btn btn-white mb-2">
                                    <i class="bi bi-eye-fill"></i>
                                </a>
                                <a href="{{ path('app_back_user_profil_edit', {'id': app.user.id}) }}" class="btn btn-white mb-2">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
