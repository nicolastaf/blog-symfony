{% extends 'back.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="col-12 p-3">
        <h1 class="h3">Catégorie : {{ category.name }}</h1>
    </div>
     <div class="col-12">
        <div class="card card-body">
            <div class="col sm-5 m-3 d-flex justify-content-between">
                <a href="{{ path('app_back_category_index') }}" class="btn btn-warning mb-2">Retour</a>

                <a href="{{ path('app_back_category_edit', {'id': category.id}) }}" class="btn btn-warning mb-2">
                    <i class="bi bi-pencil-square"></i>Modifier
                </a>
            </div>
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ category.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ category.name }}</td>
                    </tr>
                    <tr>
                        <th>Slug</th>
                        <td>{{ category.slug }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

    {{ include('back/category/_delete_form.html.twig') }}
</div>
{% endblock %}
